---
title: 현대 포트폴리오 이론
description: 기대수익률과 위험, 상관계수와 분산효과, 마코위츠 모형, 효율적 프론티어, 자본배분선을 학습합니다.
sidebar:
  order: 3
learning_objectives:
  - 포트폴리오의 기대수익률과 분산을 공식으로 계산할 수 있다
  - 상관계수가 분산효과에 미치는 영향을 설명할 수 있다
  - 효율적 프론티어와 최소분산포트폴리오의 의미를 이해한다
  - 자본배분선(CAL)과 접선포트폴리오의 관계를 설명할 수 있다
estimated_minutes: 40
references:
  - title: Markowitz, H. (1952). Portfolio Selection. Journal of Finance.
    url: https://doi.org/10.2307/2975974
    accessed: "2026-03-26"
  - title: 한국금융투자협회 투자자산운용사 학습 교재
    url: https://license.kofia.or.kr
    accessed: "2026-03-26"
---

## 기대수익률과 위험

개별 자산의 **기대수익률** 은 각 시나리오별 수익률과 확률의 가중평균입니다:

$$E(R_i) = \sum_{s} p_s \cdot R_{i,s}$$

**위험(Risk)** 은 표준편차 $\sigma_i$로 측정합니다:

$$\sigma_i^2 = \sum_{s} p_s \cdot [R_{i,s} - E(R_i)]^2$$

두 자산 간의 **공분산**:

$$\text{Cov}(R_i, R_j) = \sum_{s} p_s \cdot [R_{i,s} - E(R_i)][R_{j,s} - E(R_j)]$$

**상관계수**:

$$\rho_{ij} = \frac{\text{Cov}(R_i, R_j)}{\sigma_i \sigma_j}$$

상관계수는 $-1 \leq \rho_{ij} \leq +1$ 범위를 가집니다.

## 포트폴리오의 기대수익률과 분산

두 자산 포트폴리오(비중 $w_1$, $w_2 = 1 - w_1$):

$$E(R_p) = w_1 E(R_1) + w_2 E(R_2)$$

$$\sigma_p^2 = w_1^2\sigma_1^2 + w_2^2\sigma_2^2 + 2w_1w_2\rho_{12}\sigma_1\sigma_2$$

## 상관계수와 분산효과

상관계수에 따른 분산효과:

| 상관계수 | 분산효과 | 포트폴리오 위험 |
|----------|---------|---------------|
| $\rho = +1$ | 없음 | 가중평균과 동일 |
| $-1 < \rho < +1$ | 부분적 분산 | 가중평균보다 작음 |
| $\rho = 0$ | 독립적 분산 | 상당한 위험 감소 |
| $\rho = -1$ | 완전 분산 | 위험 완전 제거 가능 |

상관계수가 낮을수록 분산효과가 커지며, **$\rho = -1$** 일 때 특정 비중에서 포트폴리오 위험을 0으로 만들 수 있습니다.

## 마코위츠 평균-분산 모형

**해리 마코위츠(Harry Markowitz)** 는 투자자가 기대수익률과 분산만으로 포트폴리오를 평가한다고 가정하였습니다. 투자자는 동일한 위험 수준에서 높은 수익률을, 동일한 수익률에서 낮은 위험을 선호합니다(**평균-분산 기준**).

## 효율적 프론티어와 최소분산포트폴리오

**효율적 프론티어(Efficient Frontier)** 는 주어진 위험 수준에서 최대 기대수익률을 제공하는 포트폴리오들의 집합입니다. 효율적 프론티어 아래에 위치한 포트폴리오는 **지배당한(Dominated)** 포트폴리오입니다.

**최소분산포트폴리오(MVP, Minimum Variance Portfolio)** 는 두 자산 포트폴리오에서 분산을 최소화하는 비중입니다:

$$w_1^* = \frac{\sigma_2^2 - \text{Cov}(R_1, R_2)}{\sigma_1^2 + \sigma_2^2 - 2\text{Cov}(R_1, R_2)}$$

## 무위험자산 결합과 자본배분선(CAL)

무위험자산(수익률 $R_f$, 분산 0)과 위험 포트폴리오 $P$를 결합하면 **자본배분선(CAL, Capital Allocation Line)** 이 형성됩니다:

$$E(R_C) = R_f + \frac{E(R_P) - R_f}{\sigma_P} \cdot \sigma_C$$

CAL의 기울기는 **샤프 비율(Sharpe Ratio)** 입니다:

$$\text{Sharpe Ratio} = \frac{E(R_P) - R_f}{\sigma_P}$$

## 접선포트폴리오

효율적 프론티어와 CAL이 접하는 지점이 **접선포트폴리오(Tangency Portfolio)** 입니다. 이 포트폴리오는 샤프 비율을 최대화하는 위험자산 포트폴리오이며, 모든 투자자가 동일한 기대를 가진다고 가정하면 **시장포트폴리오(Market Portfolio)** 와 일치합니다(**분리정리, Separation Theorem**).

투자자는 자신의 위험선호도에 따라 무위험자산과 접선포트폴리오의 비중만 조정하면 됩니다.
